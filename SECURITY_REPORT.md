# BarcodeTenko2 セキュリティ診断・リスク分析レポート

**文書番号**: SEC-REPORT-2026-001  
**対象システム**: `BarcodeTenko2` (Client: `Tenko.Native` / Server: `TenkoServer`)  
**作成日**: 2026年8月21日  
**評価ステータス**: 設計・実装段階における網羅的脆弱性レビュー (防御的改善提案)

---

## 1. エグゼクティブサマリー (Executive Summary)

本レポートは、バーコード点呼システム（`BarcodeTenko2`）および点呼集約・通知サーバー（`TenkoServer`）におけるセキュリティ上の潜在的欠陥、設計上のトレードオフ、および運用上のリスクを包括的にまとめたものです。

本システムはオフラインでの高速な点呼動作や簡単な配布を重視して設計されていますが、**「クライアント側に秘密情報を埋め込むアーキテクチャ」**や**「Web管理画面におけるHTMLエスケープ処理」**、**「単一APIキーによる認証」**などにセキュリティ上の注意点が存在します。

### 📊 リスク評価マトリクス

| 識別子 | 脆弱性 / リスク項目 | 重要度 | 影響範囲 | 悪用難易度 | 対応状況 |
|---|---|:---:|:---:|:---:|:---:|
| **VULN-01** | クライアント逆コンパイルによる暗号化鍵・APIキーの抽出 | **High** | 個人情報・API全体 | 低 (ツール使用) | 未対応 (構造的課題) |
| **VULN-02** | 管理画面フロントエンドにおける蓄積型 XSS (Stored XSS) | **High** | 管理者セッション | 中 | **✅ 修正済み** |
| **VULN-03** | 管理者パスワードの平文管理およびデフォルト値運用のリスク | **Medium** | 管理画面 | 低 | 未対応 |
| **VULN-04** | 単一共有 API キーによる全端末認証と端末個別失効の欠如 | **Medium** | データ整合性 | 低 | 未対応 |
| **VULN-05** | Power Automate Webhook URL の不正利用リスク | **Medium** | メール送信インフラ | 中 | 未対応 |
| **VULN-06** | バーコード物理仕様・フォーマットのなりすまし容易性 | **Medium** | 点呼記録 | 低 | 未対応 |
| **VULN-07** | サーバー側 SQLite データベースの平文保存 | **Low** | サーバーローカル | 高 (要OS権限) | 未対応 |

---

## 2. 脆弱性・セキュリティ上の欠陥の詳細

### 🔴 VULN-01: クライアント逆コンパイルによる暗号化鍵・APIキーの抽出 (High)

#### 【詳細】
- `Tenko.Native` はビルド時に `EmbeddedStudentsPassphrase.g.cs`（難読化パスフレーズ）および `EmbeddedServerConfig.g.cs`（APIキー・サーバーURL）を C# バイナリ内に直接コンパイルしています。
- .NET アプリケーションは中間言語（IL）で構成されているため、`ILSpy` や `dnSpy` などのフリーツールで逆コンパイルすると、**C# ソースコードとほぼ同等の形で復号ロジック・難読化解除コード・平文 API キーが閲覧・抽出可能**です。

#### 【影響】
1. 配布された `Tenko.Native.exe` と `data/students.enc` を入手した悪意ある第三者は、パスフレーズを抽出して暗号化マスタを復号し、**全校生徒の学籍番号・氏名・出席番号（個人情報）を取得**できます。
2. 抽出した API キーを使って、不正な点呼データ（偽のスキャン記録）をサーバーへ無制限に POST 送信できます。

---

### 🟢 VULN-02: 管理画面フロントエンドにおける蓄積型 XSS (Stored XSS) (High) - 【✅ 修正済み】

#### 【詳細】
- `src/TenkoServer/wwwroot/js/app.js` の `loadScans()`, `loadUnverified()`, `loadLogs()` では、サーバーから返されたレコードデータ（`r.studentName`, `r.location`, `r.barcode` 等）を `innerHTML` を使って直接 DOM に挿入していました。

#### 【対応内容】
- `app.js` に `escapeHtml()` 関数を導入し、`innerHTML` に挿入されるすべての動的データ（生徒名、学籍番号、クラスコード、場所、バーコード、メールアドレス、エラーメッセージ）を適切にサニタイズ（エスケープ）するように修正完了しました。これにより Stored XSS による管理者セッション奪取のリスクを排除しました。

---

### 🟡 VULN-03: 管理者パスワードの平文管理およびデフォルト値運用のリスク (Medium)

#### 【詳細】
- `src/TenkoServer/appsettings.json` 内に `AdminPassword: "admin"` が平文で定義されており、起動時にハッシュ化（PBKDF2 / Argon2 / bcrypt 等）されずに平文比較（`string == string`）されています。
- 設定ファイルが誤って Git 等に公開された場合や、デフォルトの `"admin"` のままインターネット上にデプロイされた場合、誰でも管理画面に侵入可能です。
- また、パスワード比較が定時間比較（`FixedTimeEquals`）ではないため、タイミング攻撃（Timing Attack）に対して理論的な脆弱性があります。

---

### 🟡 VULN-04: 単一共有 API キーによる全端末認証 (Medium)

#### 【詳細】
- `ApiKeyAuthMiddleware.cs` では、すべてのクライアント端末に対して **1つの共通 API キー（`X-API-Key`）** を用いて認証しています。
- 特定の端末（PC）が紛失・盗難に遭った場合や、キーが漏洩した場合に、**「該当の端末だけをアクセス遮断・失効させる」ことが不可能**であり、キー変更時には運用中のすべての点呼端末バイナリをリビルド・再配布する必要があります。

---

### 🟡 VULN-05: Power Automate Webhook URL の不正利用リスク (Medium)

#### 【詳細】
- Power Automate の「HTTP 要求の受信時」トリガーで生成される URL は、URL 自体に認証トークン（Signature）が含まれる形式が多く、URL を知っていれば誰でも POST リクエストを送信できます。
- サーバーの `appsettings.json` やログから Webhook URL が漏洩した場合、第三者が任意の学生アドレス宛てにスパムメールやフィッシングメールを送信する踏み台として悪用されるリスクがあります。

---

### 🟡 VULN-06: バーコード物理仕様・フォーマットのなりすまし容易性 (Medium)

#### 【詳細】
- バーコードリーダー（USB HID）はキーボード入力として振る舞うため、学生証のバーコードフォーマット（5桁または10桁の数字）を知っていれば、手動入力やスマートフォンのバーコード画面提示等によって、**不在の生徒の点呼を容易になりすまし（代理点呼）可能**です。
- クライアント側では物理的な学生証の実体確認やワンタイム署名（TOTP / Challenge-Response）等の検証機構は存在しません。

---

### 🟢 VULN-07: サーバー側 SQLite データベースの平文保存 (Low)

#### 【詳細】
- `TenkoServer` は点呼データや通知ログをローカルの SQLite データベース（`tenko_server.db`）に暗号化せず平文で保存しています。
- サーバーホストのファイルアクセス権限（パーミッション）が適切に制限されていない場合、サーバー上の他ユーザーや不正侵入者によって全点呼ログが直接取得されるリスクがあります。

---

## 3. 推奨される改善策・ロードマップ (Remediations & Recommendations)

### 🛠️ フェーズ 1: 即時対応可能な修正 (Short-Term / Easy Fix)

1. **管理画面フロントエンドの XSS 対策 (VULN-02)**
   - `app.js` に HTML エスケープ関数（`escapeHtml`）を導入し、`innerHTML` に挿入するすべての動的文字列（氏名、コード、場所、バーコード）をエスケープ処理する。
   ```javascript
   function escapeHtml(str) {
       if (!str) return '';
       return String(str)
           .replace(/&/g, '&amp;')
           .replace(/</g, '&lt;')
           .replace(/>/g, '&gt;')
           .replace(/"/g, '&quot;')
           .replace(/'/g, '&#039;');
   }
   ```
2. **管理者パスワードのハッシュ化 & 定時間比較 (VULN-03)**
   - `appsettings.json` にはソルト付きハッシュ（BCrypt / PBKDF2）を保持させ、認証時は `CryptographicOperations.FixedTimeEquals` で定時間比較を行う。
   - 初回起動時にデフォルトパスワード（`admin`）の変更を強制する、または環境変数で安全に注入する。
3. **Power Automate 連携のシークレットヘッダー検証 (VULN-05)**
   - Power Automate 側のフローで、HTTP 要求のヘッダーに特定のリクエストヘッダー（例: `X-Webhook-Secret: <ランダム文字列>`）を付与させ、一致しないリクエストを条件分岐で即座に拒否（終了）する。

---

### 🏗️ フェーズ 2: アーキテクチャの根本的改善 (Medium / Long-Term)

1. **学生マスタ配布の廃止とサーバー側解決の完全移行 (VULN-01 根本対策)**
   - `Tenko.Native` に `students.enc`（および復号キー）を持たせるのではなく、**端末側はスキャンしたバーコード番号のみをローカル保存 & サーバーへ送信**する。
   - 氏名や出席番号の解決・管理はサーバー（`TenkoServer`）のみで完結させることで、クライアント配布物からの個人情報漏洩リスクを完全にゼロ化する。
2. **端末ごとの個別 API トークン発行 & 失効管理 (VULN-04)**
   - 管理画面から「拠点端末（Terminal）の追加・トークン発行」を行えるようにし、端末ごとに一意のトークンを割り振る。
   - 盗難や紛失が発生した端末のトークンのみを管理画面からワンクリックで失効（Revoke）できるようにする。
3. **SQLite データベースの暗号化 / パーミッション制御 (VULN-07)**
   - `SQLCipher`（EF Core SQLite 暗号化拡張）を利用してデータベースファイルを暗号化する、または Linux サーバー上で `chmod 600 data/tenko_server.db` として Web 実行ユーザー以外からの読み取りを厳格に遮断する。

---

## 4. 総括

| 観点 | 評価 | 改善後の姿 |
|---|---|---|
| **クライアント配布** | 鍵が埋め込まれているため、悪意ある学生や外部者による逆コンパイルで情報抽出が可能 | 端末からは学生マスタと秘密鍵を撤廃し、番号のみを扱うシンクライアント化 |
| **Web 管理画面** | `innerHTML` による XSS リスク、平文パスワード | HTML エスケープ徹底、パスワードハッシュ化、CORS・CSP ヘッダーの適用 |
| **API 通信** | 単一 API キーによる全拠点共通認証 | 端末ごとの個別トークン認証と失効管理 |

現在のシステムは学内クローズド環境での実用性とポータビリティに優れていますが、本レポートで指摘した **XSS 対策** や **パスワード管理の強化**、将来的な **シンクライアント化（マスタのサーバー一元管理）** を適用することで、教育機関の個人情報を守るセキュアなシステムへと堅牢化できます。
