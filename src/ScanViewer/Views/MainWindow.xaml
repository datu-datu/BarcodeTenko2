<Window x:Class="ScanViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScanViewer"
        xmlns:vm="clr-namespace:ScanViewer.ViewModels"
        mc:Ignorable="d"
        Title="Scan Data Viewer" Height="700" Width="900"
        Background="#F0F2F5">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,6"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Area -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Scan Data Viewer" FontSize="20" FontWeight="Bold" Foreground="#1A237E"/>
                <TextBlock Text="{Binding SelectedFilePath}" FontSize="11" Foreground="#666" Margin="0,2,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="ファイルを開く" Command="{Binding SelectFileCommand}" 
                    Style="{StaticResource HeaderButtonStyle}" VerticalAlignment="Center"/>
        </Grid>

        <!-- Global Search Bar -->
        <Border Grid.Row="1" Background="White" CornerRadius="4" Padding="10" Margin="0,0,0,15" BorderBrush="#DDD" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="検索:" VerticalAlignment="Center" Margin="5,0,15,0" FontWeight="SemiBold" Foreground="#555"/>
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="5" VerticalContentAlignment="Center" Background="Transparent" BorderThickness="0,0,0,1" BorderBrush="#CCC"/>
                <Button Grid.Column="2" Content="クリア" Command="{Binding ClearSearchCommand}" 
                        Margin="10,0,0,0" Padding="10,2" Background="#EEE" BorderBrush="#CCC"/>
            </Grid>
        </Border>

        <!-- Main Content (Tabs) -->
        <TabControl Grid.Row="2" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="15,8"/>
                    <Setter Property="FontSize" Value="13"/>
                </Style>
            </TabControl.Resources>
            
            <!-- Tab 1: Detailed List -->
            <TabItem Header="詳細リスト">
                <Border Background="White" CornerRadius="0,4,4,4" BorderBrush="#DDD" BorderThickness="1">
                    <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" 
                              IsReadOnly="True" GridLinesVisibility="Horizontal" Background="White" 
                              BorderThickness="0" RowHeight="28" FontSize="12" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="No." Binding="{Binding Index}" Width="45">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Foreground" Value="#888"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="学籍番号" Binding="{Binding Last5, StringFormat=\{0:D5\}}" Width="100"/>
                            <DataGridTextColumn Header="氏名" Binding="{Binding StudentName}" Width="*"/>
                            <DataGridTextColumn Header="コード" Binding="{Binding StudentCode}" Width="120"/>
                        </DataGrid.Columns>
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#FAFAFA"/>
                                <Setter Property="Padding" Value="8,4"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#EEE"/>
                            </Style>
                        </DataGrid.Resources>
                    </DataGrid>
                </Border>
            </TabItem>

            <!-- Tab 2: Dense Grid -->
            <TabItem Header="簡易グリッド">
                <Border Background="White" CornerRadius="0,4,4,4" BorderBrush="#DDD" BorderThickness="1" Padding="10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Items}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel IsItemsHost="True" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" 
                                            CornerRadius="2" Margin="3" Padding="6,3" MinWidth="55">
                                        <TextBlock Text="{Binding Last5, StringFormat=\{0:D5\}}" 
                                                   FontSize="12" FontFamily="Consolas, Monospace" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Foreground="#333"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
